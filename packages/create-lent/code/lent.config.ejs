import { defineConfig } from "lent";
import {
  lentLeetCode,
  injectLeetCodePlugin,
  injectQueryPlugin,
} from "@lent/leet-code";

export default defineConfig({
  port: 3000,
  root: "./src",
  middleware: [lentLeetCode],
  plugins: [
    injectQueryPlugin({
      include: "./index.ts",
    }),
    injectLeetCodePlugin(),
  ],
});